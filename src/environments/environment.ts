export const environment = {
  production: false,
  emailjs: {
    serviceId: 'service_lk6ldwe', // Fallback - override with EMAILJS_SERVICE_ID in production
    templateId: 'template_fs88ytr', // Fallback - override with EMAILJS_TEMPLATE_ID in production
    publicKey: 'B9Y4cqffouaVO2SfU' // Fallback - override with EMAILJS_PUBLIC_KEY in production
  }
}; 